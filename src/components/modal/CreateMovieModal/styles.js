import {StyleSheet}  from 'react-native';

const styles = StyleSheet.create({
  closeButton: {
    marginTop       : 50,
    marginLeft      : 10,
  },
  headerTitle : {
     alignSelf      : 'center',
     marginTop      : 40,
     fontSize       : 20
  }
});
export default styles;
